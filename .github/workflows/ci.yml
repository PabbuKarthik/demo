name: Docker Image Scan

on:
  push:
    branches: [ main ]

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t demo-node .

      - name: Scan Docker image
        run: docker scan demo-node || true
